# Atlas 实施细则

编制日期：2026-09-13  
依据：[MonSite Atlas — Codex 实施交接文档](./MonSite%20Atlas%20—%20Codex%20实施交接文档.md)

本文将交接文档收敛为六个可顺序执行、分别验收的步骤。目标是交付可以公开浏览、私人维护、长期保存数据的个人地理档案网站。

当前进度：第一至第四步已完成并通过本地验收；第五至第六步尚未实施。生产 D1、Cloudflare Access Application、Worker 和正式域名仍未创建或部署。

## 项目现状与实施边界

- 工作目录为 `/Users/guillaume/Guillaume/Developer/shenhaike`。Home、Ink、Lens 是该目录内的三个独立 Git 仓库；检查时工作区均干净，均使用 Astro 5.13.10。
- Atlas 已在现有 `shenhaike-atlas` 位置初始化为独立 Git 仓库和单一 Astro Worker 应用，没有移动或改造其他子站。
- Home、Lens 的 README 尚未包含部署流程；本地未发现可直接复用的 Wrangler 配置。生产 Cloudflare 资源和域名状态需在部署时核实。
- Atlas 已固定 Node 24.21.0、pnpm 12.4.1、Astro 7.3.2、Cloudflare 适配器 14.3.1 和 Wrangler 4.131.1，没有批量升级其他子站。
- 初版完成地图、地点、旅程、时间轴、私人后台、D1 和正式部署。暂缓 R2 上传、Ink/Lens 内容关联、搜索、附近地点、距离计算、高级统计和国际化。
- 初版数据保存即公开，不引入草稿发布流程。私人后台代表管理入口受保护，不代表保存的地点或访问记录是私密内容。

## 第一步：建立应用骨架，打通本地 D1

**实施内容**

- 初始化 Astro、TypeScript、Cloudflare 适配器和 Wrangler；采用一个 Astro 应用、一个 Worker，同时承载公开页面、后台和服务端接口。
- 建立页面、布局、组件、样式、共享类型、数据库访问、服务端验证、地图适配和 migrations 的必要目录，避免额外服务或通用框架。
- 配置 D1 binding，明确区分本地与生产。开发和测试默认使用 local D1，生产操作使用明确命名的独立命令。
- About 和静态资源保持静态；首页、地点、旅程和时间轴等数据库内容采用按请求渲染。初版不缓存动态数据响应，后台保存成功后下一次读取即获得新数据；不要求已打开页面自动推送更新。
- 公开页面在服务端直接复用查询层；仅为地图交互等真实需要提供公开只读 API，避免页面通过 HTTP 调用自己的 API。
- 从 Home、Lens 提取字体、留白、低饱和色和细线等视觉原则，在 Atlas 内实现独立布局，不引入跨仓库共享包。
- 固定依赖和锁文件，提供安装、开发、检查、构建、本地数据库初始化命令；忽略 secrets、本地数据库和构建产物。

**交付与验收**

- [x] 本地应用可启动，测试页面能读取 local D1。
- [x] 无生产数据库凭据也能运行开发环境。
- [x] `pnpm check`、`pnpm build` 通过。
- [x] README 记录运行时、包管理器和初始启动流程。

## 第二步：完成数据模型与业务规则

**实施内容**

- 使用 migration 创建 `places`、`journeys`、`visits`，字段以交接文档为基础；Place 和 Journey 的 slug 各自唯一，Visit 必须关联有效的 Place 和 Journey。
- 坐标统一保存为 WGS84 经纬度，纬度范围为 -90 至 90，经度范围为 -180 至 180。地图如需其他坐标格式，只在地图适配层转换。
- Visit 日期采用 `YYYY-MM-DD`，表示地点当地的日历日期；创建和更新时间使用 UTC 时间戳。校验真实日历日期、旅程起止顺序和 Visit 是否位于旅程日期范围内。调整旅程日期时，同样检查已有 Visits。
- 同一 Place 可以在不同旅程或同一旅程中重复访问，不对地点与旅程的组合施加唯一限制。
- Journey 按 `sequence` 展示访问顺序；Timeline 按访问日期倒序排列，同日记录使用稳定的次级排序。顺序整体提交并原子更新，避免部分保存。
- 有 Visit 引用的 Place 禁止删除，并返回可理解的关联提示。删除 Journey 时明确告知会删除其 Visits，确认后整体删除，但保留 Places。
- 查询使用 prepared statements 和参数绑定；服务端统一校验必填项、slug、字符串长度、日期、坐标和外键，数据库承担最终唯一性和完整性约束。
- 为 Visit 的地点、旅程顺序和日期查询设置必要索引。共享领域类型和输入类型，区分表单输入与数据库记录。
- Cover 允许为空；不创建空壳媒体系统或内容关联表。未来以 migration 增加 R2 或 `content_links`，不复制 Ink/Lens 完整内容。
- 提供本地 seed，覆盖多个地点、重复访问和跨年旅程；额外验证完全空库状态。seed 不自动进入生产。

**交付与验收**

- [x] 空数据库可通过 migration 和 seed 建立可用测试数据。
- [x] 重复 slug、非法坐标、无效日期及不存在的外键均被拒绝。
- [x] 重复到访正常保存，删除不会产生孤儿记录。
- [x] 排序保存具有原子性，失败后不会留下部分新顺序。

## 第三步：完成私人后台与写入权限

**实施内容**

- 实现 `/guillaume` 工作台，显示 Places、Journeys、Visits 数量与最近更新，并提供新建入口。
- 完成 Place 列表、新建、编辑、删除；Journey 列表、新建、编辑、删除。
- Journey 编辑页可选择已有 Place、添加或修改 Visit 日期及备注、上下移动访问顺序、移除 Visit。第一版不做拖拽排序。
- 写接口集中在 `/guillaume/api/*`，与 `/guillaume` 和 `/guillaume/*` 一同受 Cloudflare Access 保护；公开 `/api/*` 如有需要，仅提供读取能力。
- 服务端统一验证 Access JWT 的签名、签发方、受众和有效期，并限制管理员身份。生产认证配置缺失时拒绝后台请求，不能只检查请求头存在或前端登录状态。
- 写操作只接受明确的非 GET 方法，校验请求来源，防止第三方站点触发修改。身份错误、字段错误和数据冲突使用一致且可读的错误响应，不向客户端暴露 SQL 或堆栈。
- 本地测试身份仅在明确的本地开发环境启用，不能通过请求参数或 Host 头打开生产认证旁路。
- 表单显示提交中、成功和失败状态；失败保留输入；防止重复提交；删除前显示影响范围并确认。
- 后台页面和响应禁止缓存，设置禁止索引，不进入公共导航或 sitemap。

**交付与验收**

- [x] 无需手写 SQL，即可新建地点、建立旅程、添加访问并调整顺序。
- [x] 错误输入可定位到字段，保存失败不丢失已填内容。
- [x] 匿名、伪造身份、过期身份和不可信来源的写请求均不能修改数据。
- [x] 生产认证缺失时后台关闭，本地开发方式不会在生产生效。

## 第四步：完成公开档案页面与数据联动

**实施内容**

| 路由 | 初版功能 |
| --- | --- |
| `/` | 地图区域、旅程、最近到访地点、数据库统计 |
| `/places/[slug]` | 中英文名、描述、坐标、地区、历次访问与旅程链接 |
| `/journeys` | 按年份组织的旅程列表、时长与地点数量 |
| `/journeys/[slug]` | 按访问顺序展示地点、日期、备注和详情链接 |
| `/timeline` | 从 Visits 生成按年月倒序排列的时间轴 |
| `/about` | 简短产品介绍与 Home、Ink、Lens 链接 |

- 所有页面复用数据查询和展示组件；地图在本步接入页面容器，在第五步完成交互。
- 最近地点按最后访问日期排序并去重；旅程地点数使用不同 Place 数量，Visit 数量单独计算；旅程天数按日历日期计算并包含首尾两天。
- 首页 Places 统计已被访问的不同地点，Journeys 统计至少包含一次访问的旅程，Regions 按已访问地点的国家与地区组合去重并排除空值。后台数量统计全部记录。
- 无 Visit 的 Place 和空 Journey 仍可通过详情页访问并显示空状态，但不进入首页已到访统计或地图；Journey 列表可展示空旅程。
- 导航采用 Map、Journeys、Timeline、About 和 Home；不出现后台入口。使用坐标、序号、时间和路线建立 Atlas 的视觉身份。
- 页面提供标题、描述、规范 URL、空状态、真实 404 和数据库故障页，故障时不展示内部异常。

**交付与验收**

- [x] 可以从地点进入旅程，再从旅程或时间轴回到地点。
- [x] 后台新增、修改或删除访问后，下一次读取的公开页面数据一致更新。
- [x] 重复访问不会重复增加地点统计，跨年记录归属正确。
- [x] 无数据、未知 slug 和数据库不可用均有适当状态与页面。

## 第五步：完成地图和移动端体验

**实施内容**

- 首页与 `/map` 共用地图组件。建立统一 `MapPoint`，至少包含地点标识、名称、经纬度、详情 URL 和访问摘要，业务页面不依赖第三方 SDK 的专用格式。
- 在第一轮地图实现中，用 seed 验证低对比底图、缩放、点位选择、点位密集场景和手机触控；根据实测固定地图实现与底图来源，并记录许可、署名和坐标处理要求。
- `/map` 提供 All、Year、Journey 三种筛选能力，年份根据 Visit 日期判断；年份与旅程同时选择时取交集；结果按 Place 去重。
- 初始视野适配有效点位；零点位显示空状态，一个点位使用适当缩放；同坐标或密集点位始终可通过同步列表选择。
- 桌面提供悬停摘要与点击入口；手机通过点击显示地点信息并进入详情，不依赖 hover。提供键盘可用的筛选和地点列表。
- 地图失败时保留地点列表和页面导航。手机采用地图与列表组合，避免仅缩小桌面布局。
- Journey 使用简化连线表达访问顺序，不标称真实道路、GPS 轨迹或行驶距离。
- 动效保持克制，并尊重减少动态效果的系统偏好。

**交付与验收**

- [ ] 地图与列表筛选一致，重复到访只显示一个地点点位。
- [ ] 桌面、手机和键盘都能选中地点并进入详情。
- [ ] 零点、单点、密集点及地图资源加载失败均可正常浏览。
- [ ] 地图依赖及底图来源已经确定，必要署名清晰可见。

## 第六步：完成验证、上线与维护交接

**实施内容**

- 执行完整业务链：新建地点 → 新建旅程 → 添加访问 → 排序 → 公开查看 → 修改 → 验证关联删除。
- 对数据库 CRUD、权限和核心数据规则进行针对性测试；对公开页面、后台表单、移动布局和可访问性做浏览器检查，不为纯文案或间距建立复杂测试。
- 运行 Atlas 实际提供的质量检查和构建命令，记录结果；只有配置了 lint 时才要求执行 lint，不复用其他子站会自动改写文件的脚本。
- 核实 Cloudflare 账号、域名、Access 管理员身份和资源权限；创建生产 D1，执行本地已验证的 migration。生产不导入 seed。
- 部署 Worker 并绑定 `atlas.shenhaike.com`，配置仅本人可访问的 Access 策略。正式入口启用前完成后台保护；同时检查默认 Worker 域名与预览入口无法绕过服务端验证。
- 通过后台录入首批真实内容，检查未登录公开浏览、本人后台登录、数据更新及未知页面状态。公开动态页面不使用会导致保存后陈旧内容的缓存规则。
- 为公开动态内容提供可更新的 sitemap，排除后台和管理接口；核实后台禁止索引响应。
- README 写明环境变量、本地初始化、migration、seed、生产部署、Access、数据库 export/restore 和故障排查。使用导出数据在隔离数据库完成一次恢复演练，不以覆盖生产作为演练方式。
- 生产已有真实数据后，结构变更前先导出备份；应用回退与数据库恢复分别记录，不假定回退 Worker 会自动回退 schema。采用兼容性 migration，破坏性变更单独安排。
- Git 提交按功能边界拆分。最终报告实际完成的功能、架构、数据库、路由、Cloudflare 配置、验证结果和仍需人工处理的事项。

**交付与验收**

- [ ] 数据库、服务端校验、权限及核心业务测试通过，检查和生产构建通过。
- [ ] 所有公开路由及后台核心操作在桌面和手机可用。
- [ ] 未登录访客可以浏览公开网站，但不能通过任何入口写入数据。
- [ ] 本人可以通过后台维护真实数据，保存后无需重新构建或部署。
- [ ] 新设备可按 README 恢复开发环境，备份恢复流程已经在隔离环境验证。
- [ ] 正式域名、D1、Worker、Access 的完成状态和人工步骤如实记录。

## 执行约定与参考

按六步顺序推进，每步通过验收后进入下一步；权限、响应式和错误处理随对应功能完成，第六步做全链路验证。建站代码尚未开始，本文不授权将测试数据发布到生产。

依赖版本、地图实现与生产资源标识应在对应步骤中通过实际兼容性检查确定并写入 README；不得凭旧文档示例直接套用，也不要求用户替实施者选择技术库。

- [Astro Cloudflare 适配器与按需渲染](https://docs.astro.build/en/guides/integrations-guide/cloudflare/)
- [Astro 部署到 Cloudflare](https://docs.astro.build/en/guides/deploy/cloudflare/)
- [Cloudflare Access JWT 验证](https://developers.cloudflare.com/cloudflare-one/access-controls/applications/http-apps/authorization-cookie/validating-json/)
