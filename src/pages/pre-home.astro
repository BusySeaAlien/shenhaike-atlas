---
// The globe is Atlas' homepage now, served from "/".
//
// This path is kept as a permanent redirect rather than deleted: it was the
// public URL while the globe was a candidate homepage, so any link to it —
// bookmarks, shared links, the deployed Worker's history — should still land
// somewhere real.
return Astro.redirect("/", 301);
---
